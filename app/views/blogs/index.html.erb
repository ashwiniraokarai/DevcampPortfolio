<p id="notice"><%= notice %></p> <!--Ash: notice is defined in the controller action destroy -->

<h1>Blogs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= blog.title.upcase %></td>
        <td><%= blog.body %></td>
        <!-- Ash: Adding a line to display the newly added status as an html column. status is an enum defined in blog.rb model). blog.status is being
          hyperlinked to the custom route mapping to toggle_status_blog_path -->
        <td><%= link_to blog.status, toggle_status_blog_path(blog.id) %></td>
        <td><%= link_to 'Show', blog %></td>
        <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
        <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <!-- method here refers to the http VERB on thr routing tables -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Blog', new_blog_path %>
